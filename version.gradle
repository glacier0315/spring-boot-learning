ext {
    // spring cloud 和spring boot
    springBootVersion = "2.7.4"

    // shiro
    shiroStarterVersion = "1.10.0"

    // 模板布局
    thymeleafLayoutVersion = "3.1.0"

    //spring boot admin
    springBootAdminVersion = "2.7.6"
    // rocketmq
    rocketmqStarter = "2.2.2"

    // openapi
    openapiVersion = "1.6.12"
    swaggerVersion = "3.0.0"

    // 数据库相关
    druidVersion = "1.2.13"
    dynamicDatasourceVersion = "3.5.2"
    // 数据库操作
    mybatisVersion = "2.2.2"
    pagehelperVersion = "1.4.5"
    mybatisPlusVersion = "3.5.2"
    // Flyway 8.2.1及以后的版本确实是不支持MySQL
    flywayVersion= "8.2.0"

    // 加密
    jasyptStarterVersion = "3.0.4"

    // office
    easyExcelVerision = "3.1.1"
    poiVersion = "5.2.0"

    // 工作流
    activiti6Version = "6.0.0"
    activiti7Version = "7.1.0.M6"
    activitiModelerVersion = "5.23.0"

    // 工具
    commonsLang3Version = "3.12.0"
    commonsIoVersion = "2.11.0"
    hutoolVersion = "5.8.4"
}
