subprojects {
    group "com.glacier.platforms"

    // 指定仓库
    repositories {
        mavenLocal()
        maven { url "https://maven.aliyun.com/repository/public" }
        mavenCentral()
    }
}

//wrapper {
//    gradleVersion = "8.4"
//}
