spring:
  profiles:
    active: dev
  application:
    name: spring-boot-admin-server1

server:
  port: 8000


management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

